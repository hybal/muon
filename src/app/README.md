# App

This is the builtin "runner" for muon.

In general muon should be used as a library, however this exists to both have an example of how to use muon and for to allow for distribution of muon IR and conversion without having to include the compiler that generated it
